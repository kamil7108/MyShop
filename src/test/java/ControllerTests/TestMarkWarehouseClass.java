package ControllerTests;

import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import pl.polsl.lab1.shop.Model.Article;
import pl.polsl.lab1.shop.Model.MarkWarehouse;

import java.util.List;
import java.util.stream.IntStream;
import java.util.stream.Stream;

import static org.junit.Assert.assertEquals;

public class TestMarkWarehouseClass {
//    MarkWarehouse SUT =new MarkWarehouse();
//
//    static Stream<Arguments> methodSource() {
//        List <Article> params = List.of(new Article("NAME",0.0,"TEST"),new Article("NAME2",0.0,"TEST2"));
//
//        return IntStream.range(0, params.size())
//                .mapToObj(index -> Arguments.arguments(params.get(index), index));
//    }
//
//    @ParameterizedTest
//    @MethodSource("methodSource")
//    public void Test_add_article_to_warehouse(Article article,int index){
//        SUT.addArticle(article);
//        assertEquals(article,SUT.getListOfArticle().elementAt(index));
//    }
}
